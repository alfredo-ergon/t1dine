# Infrastructure

Use Bicep modules and Azure Developer CLI environments. Separate development, test, staging, and production subscriptions or resource boundaries. Production deployment is never automatic from a developer workstation or Claude Code session.

Initial services: resource group, managed identities, Key Vault, PostgreSQL, Storage, Service Bus, AI Search, Container Apps environment, Application Insights, Front Door/WAF, and optional APIM when governance needs justify it.
