Implement only the approved Slice 1: repository foundation and executable domain contracts.

Before editing, restate the approved scope, acceptance criteria, and excluded work. Then:
- validate the monorepo configuration;
- create runnable package shells only where needed;
- implement the canonical food identity and provenance contracts;
- implement synthetic fixtures;
- add repository validation, lint, typecheck, and unit-test commands;
- keep the dose engine unchanged unless the approved plan explicitly includes it;
- update the ADR and traceability records;
- run all checks and report exact results.

Do not create cloud resources, connect production data, import a live food dataset, publish packages, push branches, or modify external work items.
