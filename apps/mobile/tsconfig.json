{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "jsx": "react-jsx"
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "//": "test/ holds vitest specs (run via the root `pnpm test`); excluded here so the app typecheck doesn't require vitest as a mobile dependency.",
  "exclude": ["node_modules", "test"]
}
