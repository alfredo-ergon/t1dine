# Future Read-Only Food Catalogue MCP

Do not build this until the canonical schema and read model stabilise. It must expose only curated, non-user food data through parameterised read operations. It must never expose meal history, glucose, insulin settings, dose records, credentials, or write operations.
