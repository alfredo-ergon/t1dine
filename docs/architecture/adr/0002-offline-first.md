# ADR 0002: Offline-First Core Meal Flow

Status: Proposed

Core search subsets, saved foods, recipes, meal assembly, and recent history must operate offline. Sync uses versioned changes and explicit conflict rules. Network availability must not be a prerequisite for constructing a meal.
