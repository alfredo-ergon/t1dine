# ADR 0006: AI Outside the Clinical Calculation Path

Status: Accepted as a safety constraint

AI may produce food candidates, label extraction, translations, and search suggestions. AI output must be labelled, user-confirmed, and excluded from direct dose calculation until converted into explicit confirmed nutrition input. AI never changes profile parameters or clinical thresholds.
